第二次修改主要做出了以下改变：
1、增加了calculator的bindFunction函数，减少了原来冗余的代码，
     为每个按钮的点击添加了事件，并将每个事件抽象为函数，提高了可读性，增加了注释
2、改进了当用户输入了大量数字的体验，防止溢出
3、fifteen puzzle添加了大量注释，方便立即阅读，原来css赋值也修改为了传入一个对象，
     在洗牌程序中，将重复的代码抽象为了函数，方便每次使用的时候来调用，减少了代码量，
     也提高了可读性
4、game的两个小游戏，。。由于自己能力不太足，只做了少部分修改，感觉已经到了我的目前
     优化水平的上限了
5、table-sorter里面，说明一下就是makeSortable其实是用来写神秘代码的，其实和本程序没有关系
     修改了Array.prototype.sort.call,改为了使用sort来排序，判断字符串的大小关系的时候，使用了
     localeCompare